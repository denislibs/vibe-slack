# Client MLS orchestration 6.3b — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** Wire conversations (6.1) + KT verifier (6.2) + MLS engine (6.3a) into real flows — create conversation (with compliance), add member (key-material → KT-verify → MLS add → roster → deliver → publish GroupInfo), join public channel (external commit), and real send/receive — plus the small backend endpoints that were missing.

**Architecture:** Backend adds a per-identity key-material endpoint, GroupInfo storage (`PUT/GET /conversations/{group}/group-info`), and a compliance KeyPackage endpoint. Frontend gains a conversations API client + key-material/group-info/compliance clients, and `features` use-cases orchestrating the crypto worker, protocol worker, AS, KT, and conversation APIs. UI is 6.3c.

**Tech Stack:** Go (pgx, net/http, testcontainers), SolidJS + FSD (vitest).

**Conventions:** repos in `internal/store` (pgx, 23505→conflict, ErrNoRows→ErrNotFound); services per package take Repo interfaces; httpapi helpers `decodeJSON`/`writeJSON`/`writeError`/`sessionFrom`; routes in `NewRouterFull`; integration tests via `newTestPool(t)` (package store). Frontend API clients mirror `shared/api/workspace.ts` (Bearer per call); features mirror `features/onboard-device`/`authenticate` (dependency-injected ports, pure, unit-tested with mocks). `CryptoClient` (`shared/lib/crypto/client.ts`) has createGroup/createGroupWithCompliance/addMember/joinFromWelcome/joinByExternalCommit/exportGroupInfo/encrypt/decrypt/keyPackage/signingPublicKey. `KTVerifier.verifyIdentity(token, identity)→Uint8Array[]` (`shared/lib/kt/client.ts`). Docker required for backend integration tests.

---

### Task 1: group_info storage (migration + repo)

**Files:** Create `backend/internal/platform/postgres/migrations/0006_group_info.sql`; Modify `backend/internal/store/conversations.go`; Test add to `conversations_test.go`.

- [ ] **Step 1:** migration test (mirror `migrate_conversations_test.go`) asserting `conversation_meta` has a nullable `group_info` BYTEA column. RED.
- [ ] **Step 2:** `0006_group_info.sql`: `ALTER TABLE conversation_meta ADD COLUMN group_info BYTEA;`
- [ ] **Step 3:** add to `ConvRepo`:
```go
func (r *ConvRepo) SetGroupInfo(ctx context.Context, groupID string, info []byte) error {
	ct, err := r.pool.Exec(ctx, `UPDATE conversation_meta SET group_info=$2 WHERE group_id=$1`, groupID, info)
	if err != nil { return err }
	if ct.RowsAffected() == 0 { return ErrNotFound }
	return nil
}
func (r *ConvRepo) GroupInfo(ctx context.Context, groupID string) ([]byte, error) {
	var gi []byte
	err := r.pool.QueryRow(ctx, `SELECT group_info FROM conversation_meta WHERE group_id=$1`, groupID).Scan(&gi)
	if errors.Is(err, pgx.ErrNoRows) { return nil, ErrNotFound }
	if err != nil { return nil, err }
	if gi == nil { return nil, ErrNotFound } // not yet published
	return gi, nil
}
```
- [ ] **Step 4:** integration test in `conversations_test.go`: create channel, `GroupInfo`→ErrNotFound; `SetGroupInfo`; `GroupInfo`→bytes. Run → PASS.
- [ ] **Step 5:** Commit `feat(backend): store latest GroupInfo per conversation`.

---

### Task 2: key-material endpoint (devices + KeyPackages by identity)

**Files:** Modify `backend/internal/store/devices.go` (add `ActiveDevicesWithKeys`); Create `backend/internal/httpapi/keymaterial_handlers.go` + test; Modify `router.go`, `dto.go`.

- [ ] **Step 1:** `DeviceRepo.ActiveDevicesWithKeys(ctx, userID) ([]DeviceKey, error)` where `type DeviceKey struct { DeviceID string; SigningPublicKey []byte }`:
```sql
SELECT id, signing_public_key FROM devices WHERE user_id=$1 AND status='active' ORDER BY id
```
Integration test in `devices_test.go`. RED→GREEN.
- [ ] **Step 2:** handler `GET /workspaces/{wsId}/users/{identity}/key-material`. Deps: a small interface set — `users.FindByEmailOrUsername`, `workspace.RoleOf` (caller∈WS, target∈WS → else 404), `DeviceRepo.ActiveDevicesWithKeys`, `keypackages.Consume(deviceID)`. For each active device, consume one KeyPackage; skip devices with no available KP (or 409 if none at all). Respond `[]keyMaterialResp{ DeviceID, SigningPublicKey(b64), KeyPackage(b64) }`.
  - Build a `keyMaterialHandlers` struct holding the needed services (mirror existing handler structs). Use `sessionFrom` for caller. Resolve `identity` via `FindByEmailOrUsername`→ user; `RoleOf(wsId, caller)` and `RoleOf(wsId, targetUser)` must both succeed (else 404 not_found).
- [ ] **Step 3:** DTO `keyMaterialResp`; route in `NewRouterFull` (handler needs userRepo, wsRepo, deviceRepo, kpSvc — pass what's available; `NewRouterFull` already has wsSvc, can add deviceRepo/kpSvc or reuse devSvc/kpSvc params already present). Wire.
- [ ] **Step 4:** handler test (package httpapi, fakes for the interfaces + `withSession`): target not in WS → 404; happy path returns per-device material with signing keys + consumed KPs; caller not in WS → 404. RED→GREEN. (cmd/server may go red until Task 3/route wiring lands the new constructor args — keep httpapi tests green in isolation; defer cmd/server build to the end of Task 3.)
- [ ] **Step 5:** Commit `feat(backend): per-identity key-material endpoint for group adds`.

---

### Task 3: group-info + compliance endpoints + wiring

**Files:** Create `backend/internal/httpapi/groupinfo_handlers.go` + test; Modify `router.go`, `dto.go`, `cmd/server/main.go`, `config`.

- [ ] **Step 1:** handlers using `conversations.Service` (membership gate) + `ConvRepo`:
  - `PUT /conversations/{group}/group-info` — caller must be conv member (`convSvc.IsMember`/`Get`); body `{group_info(b64)}` → `ConvRepo.SetGroupInfo`; 404 if not a member.
  - `GET /conversations/{group}/group-info` — visible if member or public+WS member (`convSvc.Get`); `ConvRepo.GroupInfo`→ `{group_info(b64)}` or 404.
  - `GET /keypackages/compliance` — if `COMPLIANCE_DEVICE_ID` env set, `kpSvc.Consume(complianceDeviceID)` → `{key_package(b64)}`; else 404 `compliance_not_configured`.
- [ ] **Step 2:** DTOs `groupInfoResp{GroupInfo string}`, `putGroupInfoReq{GroupInfo string}`, `complianceKPResp{KeyPackage string}`. Add `ComplianceDeviceID` to `config`. Routes in `NewRouterFull` (handlers need convSvc + ConvRepo + kpSvc + complianceDeviceID). Wire `cmd/server/main.go` to construct + pass everything; `go build ./... && go vet ./... && go test ./...` green.
- [ ] **Step 3:** handler tests (fakes + withSession): PUT non-member→404; PUT member→200 + stored; GET member→bytes; GET unpublished→404; compliance configured→KP, unconfigured→404. RED→GREEN.
- [ ] **Step 4:** full backend `go test ./...` green.
- [ ] **Step 5:** Commit `feat(backend): group-info storage + compliance keypackage endpoints; wire 6.3b services`.

---

### Task 4: Frontend API clients (conversations, key-material, group-info, compliance)

**Files:** Create `client/src/shared/api/conversations.ts` + test (covers 6.1 endpoints used by orchestration + the new 6.3b endpoints).

- [ ] **Step 1-4:** `ConversationsClient(baseURL, fetchFn=fetch)` (Bearer per call), methods + fetch-mock tests:
  - `create(token, wsId, body)` → POST `/workspaces/{wsId}/conversations` (dm/channel), returns `{group_id,type,visibility,name}`.
  - `addUser(token, group, emailOrUsername)` → POST `/conversations/{group}/users`.
  - `join(token, group)` → POST `/conversations/{group}/join`.
  - `members(token, group)` → GET; `list(token, wsId)` → GET.
  - `addDeviceToRoster(token, group, deviceId, joinSeq)` → POST `/conversations/{group}/members` (device roster).
  - `keyMaterial(token, wsId, identity)` → GET `/workspaces/{wsId}/users/{identity}/key-material` → `[{device_id, signing_public_key, key_package}]` (b64→Uint8Array).
  - `getGroupInfo(token, group)` / `putGroupInfo(token, group, bytes)`.
  - `complianceKeyPackage(token)` → GET `/keypackages/compliance` → Uint8Array | null (null on 404).
  Decode b64 byte fields to `Uint8Array`. Throw on non-2xx (except complianceKeyPackage/getGroupInfo may return null on 404 — your choice, document it).
- [ ] **Step 5:** Commit `feat(client): conversations + key-material + group-info API client`.

---

### Task 5: feature — createConversation

**Files:** Create `client/src/features/create-conversation/createConversation.ts` + test.

- [ ] **Steps:** `createConversation(deps, args)` where deps = `{conversations, crypto, token}` (ports). Flow:
  1. `conv = conversations.create(token, wsId, {type, visibility?, name?, emailOrUsername?})`.
  2. `compliance = conversations.complianceKeyPackage(token)`.
  3. if compliance: `crypto.createGroupWithCompliance(conv.group_id, compliance)` (returns compliance Welcome — deliver via roster/DS later) else `crypto.createGroup(conv.group_id)` (log: no compliance).
  4. `gi = crypto.exportGroupInfo(conv.group_id)`; `conversations.putGroupInfo(token, conv.group_id, gi)`.
  Return `conv`. Unit test with mocks: asserts call order; compliance-present vs absent branches; group-info published. RED→GREEN, tsc clean. Commit `feat(client): create-conversation use-case (server + MLS group + compliance)`.

---

### Task 6: feature — addMember (KT-verified)

**Files:** Create `client/src/features/add-member/addMember.ts` + test.

- [ ] **Steps:** `addMember(deps, {token, wsId, group, identity, currentMaxSeq})` where deps = `{conversations, crypto, kt, protocol}`. Flow:
  1. `conversations.addUser(token, group, identity)` (server membership).
  2. `verifiedKeys = kt.verifyIdentity(token, identity)` (Set of signing keys).
  3. `material = conversations.keyMaterial(token, wsId, identity)`.
  4. For each device in material: assert `device.signing_public_key` ∈ verifiedKeys (byte-equal to some verified key) — else throw `UnverifiedDevice` (do NOT add).
  5. For each verified device: `{commit, welcome} = crypto.addMember(group, device.key_package)`; `conversations.addDeviceToRoster(token, group, device.device_id, currentMaxSeq)`; `protocol.send` the `commit` to the group and the `welcome` to the new device (via DS — match the protocol worker's send API; Welcome delivery uses join_seq journal).
  6. `gi = crypto.exportGroupInfo(group)`; `conversations.putGroupInfo(token, group, gi)`.
  Unit test with mocks: KT-mismatch blocks add (no crypto.addMember called, throws); happy path calls add+roster+send+publish in order for each device. RED→GREEN, tsc clean. Commit `feat(client): add-member use-case with KT key verification`.

(Helper: byte-equality of Uint8Arrays; verifiedKeys compared by content.)

---

### Task 7: feature — joinPublic + inbound + send/receive slice + full verify

**Files:** Create `client/src/features/join-public/joinPublic.ts` + test; extend the app orchestrator for inbound commit/Welcome; a slice test.

- [ ] **Steps:**
  1. `joinPublic(deps, {token, group})` = `gi = conversations.getGroupInfo(token, group)`; `commit = crypto.joinByExternalCommit(gi)`; `conversations.join(token, group)` (server membership) + `addDeviceToRoster` for own device(s); `protocol.send` the external commit to the group. Unit test with mocks (order + that it throws if group-info missing).
  2. **Inbound wiring** (app orchestrator, `client/src/app/orchestrator.ts`): an incoming DS frame that is an MLS commit → `crypto.decrypt(group, bytes)` (process/merge, returns empty for commits); an incoming Welcome (new frame kind or detected) → `crypto.joinFromWelcome(bytes)`. Add a `joinFromWelcome` path; keep application-message decrypt → conversation store as is. Unit-test the orchestrator's commit/welcome handling with mocks.
  3. **Slice test** (mirror `transport-demo.test.ts`, real WASM + mock DS): alice createConversation(DM with bob) → key-material (mock returns bob's real device KP from a second engine) → KT verify (mock/stub) → addMember → alice encrypt "hi" → deliver Welcome+commit to bob engine (joinFromWelcome/process) → bob decrypt "hi". And a public variant: carol joinByExternalCommit from alice's exported GroupInfo → exchange.
- [ ] **Full verification:**
```bash
cd client && npx vitest run && npx tsc --noEmit && npx vite build
cd /Users/denisurevic/Documents/slack/backend && go build ./... && go test ./...
```
All green. Commit `feat(client): join-public + inbound commit/Welcome + live send/receive slice`.

---

## Self-Review

**Spec coverage:** group_info storage → T1; key-material → T2; group-info+compliance endpoints+wiring → T3; FE api clients → T4; createConversation → T5; addMember+KT-verify → T6; joinPublic+inbound+slice → T7. ✓ UI (6.3c), invites (7), Slack Connect (8) out of scope.

**Placeholders:** backend (T1-3) gives concrete SQL/handlers mirroring existing patterns; frontend (T4-7) gives flow + DI ports + test contracts (mirroring `features/onboard-device`), with "read the protocol worker send API / orchestrator first" notes — the slice test (T7) is the precise end-to-end contract. No vague error handling: KT-mismatch is an explicit hard stop.

**Type consistency:** `DeviceKey{DeviceID, SigningPublicKey}` (T2) ↔ key-material DTO ↔ FE `keyMaterial` decode; `ConvRepo.GroupInfo/SetGroupInfo` (T1) ↔ group-info handlers (T3) ↔ FE get/putGroupInfo (T4); `CryptoClient` methods (6.3a) ↔ createConversation/addMember/joinPublic; `KTVerifier.verifyIdentity` (6.2) ↔ addMember. `NewRouterFull` gains key-material + group-info + compliance handlers, wired in T3; cmd/server build closed at end of T3.

**Sequencing:** T2/T3 both touch router/cmd-server; T3 closes the backend build (like prior sub-plans). Frontend T4→T7 build on each other; T7 is the integration capstone with full verification.
